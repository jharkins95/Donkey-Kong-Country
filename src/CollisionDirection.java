/**
 * CollisionDirection.java
 * Used to determine the direction in which objects collide.
 * Objects cannot collide in arbitrary directions.
 * @author Jack Harkins
 *
 */
public enum CollisionDirection {
    LEFT, RIGHT, UP, DOWN;
}
